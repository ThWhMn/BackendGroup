# ??????
server:
  port: 8101

# ??????
spring:
  application:
    name: eureka-client

eureka:
  client:
    # ???Eureka?????
    register-with-eureka: true
    # ????????
    fetch-registry: true
    service-url:
      # ????????
      defaultZone: http://localhost:8001/eureka
